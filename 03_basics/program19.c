#include <stdio.h>
void averagenum(int k, int h, int n)
{
    int average;
    average = (k + h + n) / 3.0;
    printf("Average is %d", average);
}
int main()
{
    int x, y, n;
    void averagenum(int x, int y, int n);
    scanf("%d %d %d", &x, &y, &n);
    avera(x, y, n);

}

